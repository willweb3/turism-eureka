import { DividerDemo } from '@/design-system/demo/sections';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dividers | Design System | Azoreon',
  description: 'Content dividers for sections and layouts',
};

export default function DividersPage() {
  return <DividerDemo />;
}

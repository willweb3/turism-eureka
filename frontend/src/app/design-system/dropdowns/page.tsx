import { DropdownsDemo } from '@/design-system/demo/sections';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dropdowns | Design System | Azoreon',
  description: 'Dropdown and select components',
};

export default function DropdownsPage() {
  return <DropdownsDemo />;
}

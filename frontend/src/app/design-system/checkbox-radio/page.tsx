import { CheckboxRadioDemo } from '@/design-system/demo/sections';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Checkbox & Radio | Design System | Azoreon',
  description: 'Checkbox and Radio button components for selections',
};

export default function CheckboxRadioPage() {
  return <CheckboxRadioDemo />;
}
